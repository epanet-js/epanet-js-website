---
// Pressure-driven leakage demo: a PRV outlet setting steps down twice, the
// emitter-driven leakage estimate falls with it, and the downstream pipes
// shift from amber toward green as zone pressure drops.
import AppWindow from "./AppWindow.astro";
import { pipes, junctions } from "./network-geometry";

// The zone downstream of PRV-12: everything past junction [210, 75].
const zonePipes = pipes.slice(9);

const tints = [
  { color: "#f59e0b", cls: "pl-tint-a" },
  { color: "#84cc16", cls: "pl-tint-b" },
  { color: "#22c55e", cls: "pl-tint-c" },
];
---

<AppWindow
  variant="plain"
  hideHeader
  hideFooter
  label="Pressure management in epanet-js: a PRV setting stepping down while the estimated leakage falls and the downstream zone recolors from amber to green.">
  <!-- zone tint overlay: three stacked copies crossfade as pressure drops -->
  <svg
    viewBox="0 0 400 300"
    preserveAspectRatio="xMidYMid slice"
    class="absolute inset-0 w-full h-full"
    aria-hidden="true">
    <g stroke-width="2.5" stroke-linecap="round" fill="none">
      {
        tints.map((tint) =>
          zonePipes.map(([x1, y1, x2, y2]) => (
            <line
              class={tint.cls}
              x1={x1}
              y1={y1}
              x2={x2}
              y2={y2}
              stroke={tint.color}
            />
          )),
        )
      }
    </g>
    <!-- redraw the junction dots on top of the tint lines, since the tint
         overlay is a separate sibling painted after NetworkMap's own
         junctions and would otherwise cover them -->
    <g stroke="#ffffff" stroke-width="1">
      {
        junctions.map(([x, y]) => (
          <circle cx={x} cy={y} r="3.5" fill="#312e81" />
        ))
      }
    </g>
    <!-- PRV-12 at the zone inlet -->
    <g class="pl-prv">
      <rect
        x="204"
        y="69"
        width="12"
        height="12"
        rx="2"
        fill="#ffffff"
        stroke="#8b5cf6"
        stroke-width="1.5"></rect>
      <path d="M207 72 L213 78 M213 72 L207 78" stroke="#8b5cf6" stroke-width="1.2"
      ></path>
    </g>
  </svg>

  <!-- pressure management panel -->
  <div
    class="pl-panel absolute top-2 right-2 w-[208px] max-w-[70%] bg-white rounded-lg border border-gray-100 shadow-md overflow-hidden">
    <div
      class="flex items-baseline justify-between gap-2 px-3 pt-2.5 pb-2 border-b border-gray-100">
      <p class="text-xs font-semibold text-gray-700">
        Pressure management
      </p>
      <p class="text-xs text-gray-400">PRV-12</p>
    </div>
    <div class="px-3 py-2 space-y-1.5 text-xs">
      <div class="flex items-center justify-between">
        <span class="text-gray-500">Outlet setting</span>
        <span class="relative font-medium tabular-nums">
          <span class="pl-set-1 text-gray-700">52 m</span>
          <span class="pl-set-2 absolute inset-0 bg-white text-right">48 m</span>
          <span class="pl-set-3 absolute inset-0 bg-white text-right">44 m</span>
        </span>
      </div>
      <div class="flex items-center justify-between">
        <span class="text-gray-500">Est. leakage</span>
        <span class="relative font-medium tabular-nums">
          <span class="pl-leak-1 text-gray-700">6.1 l/s</span>
          <span class="pl-leak-2 absolute inset-0 bg-white text-right">
            5.4 l/s
          </span>
          <span class="pl-leak-3 absolute inset-0 bg-white text-right">
            4.8 l/s
          </span>
        </span>
      </div>
    </div>
    <div
      class="flex items-center justify-between px-3 py-1.5 border-t border-gray-100 text-xs">
      <span class="text-gray-500">Leakage savings</span>
      <span class="relative inline-block min-w-10 font-medium tabular-nums text-right">
        <span class="pl-save-1 text-gray-700">0%</span>
        <span class="pl-save-2 absolute inset-0 bg-white text-right">−11%</span>
        <span class="pl-save-3 absolute inset-0 bg-white text-right">−21%</span>
      </span>
    </div>
  </div>
</AppWindow>

<style>
  .pl-tint-a {
    animation: pl-tint-a 16s ease-in-out infinite both;
  }
  @keyframes pl-tint-a {
    0%,
    6% {
      opacity: 0;
    }
    14% {
      opacity: 1;
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }
  .pl-tint-b {
    animation: pl-tint-b 16s ease-in-out infinite both;
  }
  @keyframes pl-tint-b {
    0%,
    28% {
      opacity: 0;
    }
    36% {
      opacity: 1;
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }
  .pl-tint-c {
    animation: pl-tint-c 16s ease-in-out infinite both;
  }
  @keyframes pl-tint-c {
    0%,
    54% {
      opacity: 0;
    }
    62% {
      opacity: 1;
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }

  .pl-prv {
    animation: pl-prv-in 16s ease-out infinite both;
  }
  @keyframes pl-prv-in {
    0%,
    3% {
      opacity: 0;
    }
    8% {
      opacity: 1;
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }

  .pl-panel {
    animation: pl-panel-in 16s ease-out infinite both;
  }
  @keyframes pl-panel-in {
    0% {
      opacity: 0;
      transform: translateY(4px);
    }
    6% {
      opacity: 1;
      transform: translateY(0);
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }

  /* outlet setting steps down; the incoming value flashes violet */
  .pl-set-1 {
    animation: pl-set-1 16s linear infinite both;
  }
  .pl-set-2 {
    animation: pl-set-2 16s linear infinite both;
  }
  .pl-set-3 {
    animation: pl-set-3 16s linear infinite both;
  }
  @keyframes pl-set-1 {
    0%,
    24% {
      opacity: 1;
    }
    26%,
    100% {
      opacity: 0;
    }
  }
  @keyframes pl-set-2 {
    0%,
    24% {
      opacity: 0;
    }
    26% {
      opacity: 1;
      color: #8b5cf6;
    }
    34% {
      color: #374151;
    }
    50% {
      opacity: 1;
    }
    52%,
    100% {
      opacity: 0;
    }
  }
  @keyframes pl-set-3 {
    0%,
    50% {
      opacity: 0;
    }
    52% {
      opacity: 1;
      color: #8b5cf6;
    }
    60% {
      color: #374151;
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }

  /* leakage estimate follows each step a beat later */
  .pl-leak-1 {
    animation: pl-leak-1 16s linear infinite both;
  }
  .pl-leak-2 {
    animation: pl-leak-2 16s linear infinite both;
  }
  .pl-leak-3 {
    animation: pl-leak-3 16s linear infinite both;
  }
  @keyframes pl-leak-1 {
    0%,
    32% {
      opacity: 1;
    }
    34%,
    100% {
      opacity: 0;
    }
  }
  @keyframes pl-leak-2 {
    0%,
    32% {
      opacity: 0;
    }
    34% {
      opacity: 1;
      color: #8b5cf6;
    }
    42% {
      color: #374151;
    }
    58% {
      opacity: 1;
    }
    60%,
    100% {
      opacity: 0;
    }
  }
  @keyframes pl-leak-3 {
    0%,
    58% {
      opacity: 0;
    }
    60% {
      opacity: 1;
      color: #8b5cf6;
    }
    68% {
      color: #374151;
    }
    92% {
      opacity: 1;
    }
    98%,
    100% {
      opacity: 0;
    }
  }

  /* leakage savings tracks the leakage estimate 1:1 — same swap timing as
     pl-leak-1/2/3 above, since each savings % arrives with its matching
     leakage reading. No independent fade-in: like the other rows' first
     values, it's simply present when the panel itself fades in. */
  .pl-save-1 {
    animation: pl-save-1 16s linear infinite both;
  }
  .pl-save-2 {
    animation: pl-save-2 16s linear infinite both;
  }
  .pl-save-3 {
    animation: pl-save-3 16s linear infinite both;
  }
  @keyframes pl-save-1 {
    0%,
    32% {
      opacity: 1;
    }
    34%,
    100% {
      opacity: 0;
    }
  }
  @keyframes pl-save-2 {
    0%,
    32% {
      opacity: 0;
    }
    34% {
      opacity: 1;
      color: #8b5cf6;
    }
    42% {
      color: #16a34a;
    }
    58% {
      opacity: 1;
    }
    60%,
    100% {
      opacity: 0;
    }
  }
  @keyframes pl-save-3 {
    0%,
    58% {
      opacity: 0;
      color: #16a34a;
    }
    60% {
      opacity: 1;
      color: #8b5cf6;
    }
    68% {
      color: #16a34a;
    }
    92% {
      opacity: 1;
      color: #16a34a;
    }
    98%,
    100% {
      opacity: 0;
      color: #16a34a;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .pl-tint-a,
    .pl-tint-b,
    .pl-tint-c,
    .pl-prv,
    .pl-panel,
    .pl-set-1,
    .pl-set-2,
    .pl-set-3,
    .pl-leak-1,
    .pl-leak-2,
    .pl-leak-3,
    .pl-save-1,
    .pl-save-2,
    .pl-save-3 {
      animation: none;
    }
    .pl-tint-a,
    .pl-tint-b,
    .pl-tint-c,
    .pl-prv,
    .pl-panel {
      opacity: 1;
      transform: none;
    }
    .pl-set-1,
    .pl-set-2,
    .pl-leak-1,
    .pl-leak-2,
    .pl-save-1,
    .pl-save-2 {
      opacity: 0;
    }
    .pl-set-3,
    .pl-leak-3 {
      opacity: 1;
      color: #374151;
    }
    .pl-save-3 {
      opacity: 1;
      color: #16a34a;
    }
  }
</style>
